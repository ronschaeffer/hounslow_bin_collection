# Hounslow Bin Collection Configuration
# Test configuration using 132 Worple Rd as the example address

app:
  name: "hounslow_bin_collection"
  version: "0.1.0"
  debug: false

# Test address configuration
address:
  postcode: "TW7 7HX"
  address_hint: "132 Worple Rd"

# MQTT configuration (inherits from parent directory if available)
mqtt:
  enabled: false

# Logging configuration
logging:
  level: "INFO"
